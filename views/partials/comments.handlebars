<div class="comments">
  Comments:
  {{#each this}}
  <section class="comment">
    <div class="is-size-5">
      "{{comment_text}}"
    </div>
    <div class="meta">
      by: {{user.username}} on {{format_date created_at}}
    </div>
  </section>
  {{/each}}
</div>